# Ansible playbook lists:
- k8s-proxmox.yml
  Description: Tested and working ansible playbook deployed K8s cluster node for Ubuntu server 22.04 on Proxmox Virtual Environment 8.0.4. After usage, it requires the cluster to be initialized on one of the nodes selected as the controller
